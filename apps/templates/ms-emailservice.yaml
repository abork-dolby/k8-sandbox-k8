apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: emailservice
  namespace: default
  labels:
    project: online-boutique
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  project: platform
  source:
    path: charts/micro-service-python
    repoURL: https://github.com/abork-dolby/k8-sandbox-k8.git
    targetRevision: HEAD
    helm:
      parameters:
      - name: name
        value: "emailservice"
      - name: image.name
        value: gcr.io/google-samples/microservices-demo/emailservice
      - name: image.tag
        value: v0.2.4
      - name: "service.ports"
        value: 
        - name: grpc
          port: 5000
          targetPort: 8080
  syncPolicy:
    automated:
      prune: true
      selfHeal: true