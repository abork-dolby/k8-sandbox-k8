apiVersion: v1
kind: Service
metadata:
  name:  {{ .Values.global.name }}
spec:
  type: ClusterIP
  selector:
    app: {{ .Values.global.name }}
  ports:
  - name: grpc
    port: 5000
    targetPort: 8080